# Secrets. Never commit a filled-in env or a service-account key.
.env
*.env
!.env.example
env
client_secret*.json
*service_account*.json
*credentials*.json
token.json
xero_refresh_token

# Local state: ledger, snapshots, tombstones, evidence, local documents.
# This is operational data about real events -- it stays on the machine.
state/
localdocs/
snapshots/
tombstones/
evidence/
preview/
*.jsonl

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/

# macOS
.DS_Store

# A private letter, not part of the package.
comms/
